@page "/"
@using GestioPractiquesDuals.Application.Dashboard
@attribute [Authorize]
@inject GestioPractiquesDuals.Web.Services.DashboardApiClient DashboardApiClient

<PageTitle>Gestió de les pràctiques Duals</PageTitle>

<MudContainer MaxWidth="MaxWidth.False" Class="dashboard-shell">
    <MudStack Row="true" AlignItems="AlignItems.Center" Justify="Justify.SpaceBetween" Class="dashboard-header">
        <MudText Typo="Typo.h3" Class="welcome-title">@_dashboard.WelcomeMessage</MudText>
        <MudStack Row="true" Spacing="2" Class="dashboard-actions">
            <MudButton Variant="Variant.Outlined" Color="Color.Error" StartIcon="@Icons.Material.Filled.Bookmarks" Class="toolbar-button toolbar-button-secondary" Href="/classes/manage">GESTIÓ DE CLASSES</MudButton>
            <MudButton Variant="Variant.Filled" Color="Color.Error" StartIcon="@Icons.Material.Filled.Add" Class="toolbar-button toolbar-button-primary">NOVA ACTIVITAT</MudButton>
        </MudStack>
    </MudStack>

    <MudGrid Spacing="3" Class="kpi-grid">
        @foreach (var card in _dashboard.Stats)
        {
            <MudItem xs="12" sm="6" md="4" lg="2">
                <MudPaper Class="stat-card pa-4">
                    <MudIcon Icon="@GetMaterialIcon(card.Icon)" Size="Size.Medium" Class="stat-icon" Style="@($"color:{card.Color};")" />
                    <MudText Typo="Typo.h4" Class="stat-value" Style="@($"color:{card.Color};")">@card.Value</MudText>
                    <MudText Typo="Typo.body2" Class="stat-label">@card.Label</MudText>
                </MudPaper>
            </MudItem>
        }
    </MudGrid>

    <MudGrid Spacing="2" Class="filters-grid">
        <MudItem xs="12" md="3">
            <div class="filter-box">
                <div class="filter-label">Cercar activitat</div>
                <MudTextField T="string" Variant="Variant.Outlined" Placeholder="Cercar activitat" Adornment="Adornment.Start" AdornmentIcon="@Icons.Material.Filled.Search" FullWidth="true" Margin="Margin.Dense" />
            </div>
        </MudItem>
        <MudItem xs="12" md="3">
            <div class="filter-box">
                <div class="filter-label">Classe</div>
                <MudSelect T="string" @bind-Value="_selectedClass" Variant="Variant.Outlined" FullWidth="true" Margin="Margin.Dense">
                    @foreach (var className in _dashboard.AvailableClasses)
                    {
                        <MudSelectItem T="string" Value="@className">@className</MudSelectItem>
                    }
                </MudSelect>
            </div>
        </MudItem>
        <MudItem xs="12" md="3">
            <div class="filter-box">
                <div class="filter-label">Mòdul</div>
                <MudSelect T="string" @bind-Value="_selectedModule" Variant="Variant.Outlined" FullWidth="true" Margin="Margin.Dense">
                    @foreach (var moduleName in _dashboard.AvailableModules)
                    {
                        <MudSelectItem T="string" Value="@moduleName">@moduleName</MudSelectItem>
                    }
                </MudSelect>
            </div>
        </MudItem>
        <MudItem xs="12" md="3" Class="toggle-zone">
            <MudSwitch T="bool" Label="Només obertes" Color="Color.Default" />
            <MudSwitch T="bool" Label="Arxivades" Color="Color.Default" />
            <MudText Typo="Typo.body2" Class="toggle-count">@($"{_dashboard.ClosedActivities.Count} de {_dashboard.ClosedActivities.Count}")</MudText>
        </MudItem>
    </MudGrid>

    <MudText Typo="Typo.h5" Class="section-title">
        <MudIcon Icon="@Icons.Material.Filled.LockOpen" Size="Size.Small" Style="color:#42c96d;" />
        Activitats obertes (@_dashboard.OpenActivities.Count)
    </MudText>

    <MudGrid Spacing="3">
        @foreach (var open in _dashboard.OpenActivities)
        {
            <MudItem xs="12" md="8" lg="6">
                <MudPaper Class="activity-card pa-4">
                    <MudText Typo="Typo.h5" Class="activity-code mb-1">@open.Code</MudText>
                    <MudText Typo="Typo.body2" Class="muted activity-subtitle mb-4">@open.Subtitle</MudText>
                    <MudText Typo="Typo.body1" Class="activity-description mb-4">@open.Description</MudText>

                    <MudStack Row="true" Spacing="1" Class="mb-3">
                        @foreach (var chip in open.Chips)
                        {
                            <MudChip T="string" Variant="Variant.Outlined" Class="info-chip">@chip</MudChip>
                        }
                        @if (!string.IsNullOrWhiteSpace(open.Highlight))
                        {
                            <MudChip T="string" Color="Color.Warning" Variant="Variant.Filled" Class="highlight-chip">@open.Highlight</MudChip>
                        }
                    </MudStack>

                    <div class="progress-rail">
                        <div class="progress-fill"></div>
                    </div>

                    <MudStack Row="true" AlignItems="AlignItems.Center" Justify="Justify.SpaceBetween" Class="mt-4">
                        <MudStack Row="true" Spacing="2" Class="activity-actions">
                            <MudButton Variant="Variant.Text" Color="Color.Error" Class="activity-link activity-link-primary">RESULTATS</MudButton>
                            <MudButton Variant="Variant.Text" Color="Color.Default" Class="activity-link">GRUPS</MudButton>
                            <MudIconButton Icon="@Icons.Material.Filled.Edit" Class="activity-icon" />
                            <MudIconButton Icon="@Icons.Material.Filled.BarChart" Color="Color.Error" Class="activity-icon" />
                            <MudIconButton Icon="@Icons.Material.Filled.Campaign" Color="Color.Info" Class="activity-icon" />
                        </MudStack>
                        <MudIconButton Icon="@Icons.Material.Filled.MoreVert" Class="more-button" />
                    </MudStack>
                </MudPaper>
            </MudItem>
        }
    </MudGrid>

    <MudText Typo="Typo.h5" Class="section-title closed-title">
        <MudIcon Icon="@Icons.Material.Filled.Lock" Size="Size.Small" Style="color:#73767d;" />
        Activitats tancades (@_dashboard.ClosedActivities.Count)
    </MudText>

    <MudGrid Spacing="3">
        @foreach (var closed in _dashboard.ClosedActivities)
        {
            <MudItem xs="12" sm="6" md="4" lg="3">
                <MudPaper Class="closed-card pa-4">
                    <MudText Typo="Typo.h6" Class="closed-code">@closed.Code</MudText>
                    <MudText Typo="Typo.body2" Class="closed-title-text">@closed.Title</MudText>
                    <MudText Typo="Typo.caption" Class="muted">@closed.Subtitle</MudText>
                    <MudStack Row="true" Justify="Justify.SpaceBetween" Class="mt-6">
                        <MudIcon Icon="@Icons.Material.Filled.Badge" Style="color:#d84c3e;" />
                        <MudIcon Icon="@Icons.Material.Filled.MoreVert" Class="closed-more" />
                    </MudStack>
                </MudPaper>
            </MudItem>
        }
    </MudGrid>
</MudContainer>

@code {
    private string _selectedClass = "Totes les classes";
    private string _selectedModule = "Tots els mòduls";

    private DashboardSnapshotDto _dashboard =
        new(
            "Carregant dashboard...",
            Array.Empty<DashboardStatDto>(),
            ["Totes les classes"],
            ["Tots els mòduls"],
            Array.Empty<ActivityCardDto>(),
            Array.Empty<ActivityCardDto>());

    protected override async Task OnInitializedAsync()
    {
        _dashboard = await DashboardApiClient.GetTeacherPreviewAsync();
        _selectedClass = _dashboard.AvailableClasses.FirstOrDefault() ?? _selectedClass;
        _selectedModule = _dashboard.AvailableModules.FirstOrDefault() ?? _selectedModule;
    }

    private static string GetMaterialIcon(string iconName) =>
        iconName switch
        {
            "book" => Icons.Material.Filled.Book,
            "menu_book" => Icons.Material.Filled.MenuBook,
            "groups" => Icons.Material.Filled.Groups,
            "badge" => Icons.Material.Filled.Badge,
            "lock_open" => Icons.Material.Filled.LockOpen,
            "group_work" => Icons.Material.Filled.GroupWork,
            _ => Icons.Material.Filled.Circle
        };
}
